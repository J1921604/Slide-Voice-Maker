# Slide Voice Maker 環境変数（任意）
# start.ps1 はこのファイルが存在する場合、内容を Env: に反映します。
#
# --- TLS / 証明書（社内プロキシ対策） ---
# 1) 推奨: Windowsの証明書ストアを使う（通常はこれでOK）
SVM_USE_TRUSTSTORE=1
#
# 2) 追加のCAが必要な場合: PEMバンドル/ディレクトリを指定
#SVM_SSL_CERT_FILE=C:\path\to\corp-ca-bundle.pem
#SVM_SSL_CERT_DIR=C:\path\to\corp-ca-dir
#
# 3) 最終手段（危険）: SSL検証を無効化
#SVM_TLS_INSECURE=0
#
# --- プロキシ（必要な場合のみ） ---
#SVM_HTTPS_PROXY=http://proxy.example.local:8080
